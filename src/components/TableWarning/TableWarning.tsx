import { Typography } from '@mui/material';
import { TableContainer } from './Styles';



const TableComponent = () => {
  return (
    <TableContainer>
      <Typography variant="h4">
        Please use desktop to render application
      </Typography>
    </TableContainer>
  );
};

export default TableComponent;
